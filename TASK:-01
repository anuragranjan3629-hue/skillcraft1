<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>SkillCraft — Responsive Landing with Interactive Nav</title>

  <!-- Optional: use a system font stack — no external requests required -->
  <style>
    /* ===========================
       Reset & Base
       =========================== */
    :root{
      --bg: #0b1220;
      --panel: #071122;
      --muted: #9fb0c8;
      --accent: #06b6d4;
      --accent-2: #7c3aed;
      --glass: rgba(255,255,255,0.04);
      --white: #eaf2ff;
      --nav-height: 72px;
      --max-width: 1200px;
      --shadow: 0 8px 30px rgba(2,6,23,0.6);
      --ease: cubic-bezier(.2,.9,.2,1);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body {
      margin:0;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      color:var(--white);
      background: linear-gradient(180deg,#071022 0%, #071028 60%, #03101a 100%);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.45;
      scroll-behavior: smooth;
    }
    img{max-width:100%;display:block;height:auto}

    /* ========== NAV (fixed) ========== */
    header.site-header{
      position:fixed;
      top:0; left:0; right:0;
      height:var(--nav-height);
      display:flex;
      align-items:center;
      justify-content:center;
      z-index:9999;
      transition: background 300ms var(--ease), box-shadow 300ms var(--ease), height 300ms var(--ease), backdrop-filter 300ms var(--ease);
      background: transparent;
      backdrop-filter: none;
    }
    header.site-header.scrolled{
      background: linear-gradient(90deg, rgba(6,182,212,0.06), rgba(124,58,237,0.03));
      box-shadow: var(--shadow);
      backdrop-filter: blur(6px) saturate(120%);
    }
    .nav-wrap{
      width:100%;
      max-width:var(--max-width);
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:10px 20px;
      gap:12px;
    }

    /* brand/logo */
    .brand{
      display:flex;
      align-items:center;
      gap:12px;
      text-decoration:none;
      color:var(--white);
      font-weight:700;
    }
    .logo{
      width:42px;
      height:42px;
      border-radius:8px;
      background:linear-gradient(135deg,var(--accent), var(--accent-2));
      display:inline-flex;
      align-items:center;
      justify-content:center;
      color:#001219;
      font-weight:800;
      box-shadow: 0 6px 18px rgba(6,182,212,0.08);
    }
    .brand .title{
      display:flex;
      flex-direction:column;
      font-size:14px;
      line-height:1;
    }
    .brand .subtitle{ font-size:11px; color:var(--muted); font-weight:500; margin-top:2px }

    /* nav items */
    nav.main-nav{
      display:flex;
      align-items:center;
      gap:12px;
      margin-left:8px;
    }
    ul.nav{
      list-style:none;
      margin:0;
      padding:0;
      display:flex;
      gap:6px;
      align-items:center;
    }
    ul.nav li{ position:relative; }
    a.nav-link{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding:10px 12px;
      color:var(--white);
      text-decoration:none;
      border-radius:8px;
      font-weight:600;
      transition: transform 160ms var(--ease), background 220ms var(--ease), color 220ms var(--ease);
    }
    a.nav-link:focus{ outline:2px solid rgba(6,182,212,0.18); outline-offset:2px; }
    a.nav-link:hover{ transform:translateY(-4px); color:#001219; background:linear-gradient(90deg,var(--accent), var(--accent-2)); }

    /* active link (scrollspy) */
    a.nav-link.active{
      background: linear-gradient(90deg, rgba(6,182,212,0.12), rgba(124,58,237,0.08));
      box-shadow: 0 6px 16px rgba(6,182,212,0.04) inset;
      color:var(--white);
    }
    /* small badge on nav (demo) */
    .badge{
      background:rgba(255,255,255,0.06);
      border-radius:999px;
      padding:4px 8px;
      font-size:12px;
      color:var(--muted);
    }

    /* dropdown */
    .dropdown {
      position:relative;
    }
    .dropdown-menu{
      position:absolute;
      top:calc(100% + 10px);
      left:0;
      min-width:180px;
      background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      padding:8px;
      border-radius:10px;
      box-shadow: 0 8px 30px rgba(2,6,23,0.6);
      display:none;
      z-index:10000;
      border:1px solid rgba(255,255,255,0.03);
    }
    .dropdown:hover .dropdown-menu,
    .dropdown:focus-within .dropdown-menu {
      display:block;
    }
    .dropdown-menu a{ display:block; padding:8px 10px; border-radius:8px; text-decoration:none; color:var(--white); }
    .dropdown-menu a:hover{ background:rgba(255,255,255,0.02); transform:translateX(4px); }

    /* CTA button */
    .cta{
      padding:10px 14px;
      background:linear-gradient(90deg,var(--accent), var(--accent-2));
      color:#001219;
      border-radius:10px;
      font-weight:700;
      border:none;
      cursor:pointer;
      transition: transform 150ms var(--ease), box-shadow 200ms var(--ease);
    }
    .cta:hover{ transform:translateY(-4px); box-shadow: 0 10px 30px rgba(124,58,237,0.14); }

    /* mobile hamburger */
    .hamburger{
      display:none;
      width:44px; height:44px;
      border-radius:10px;
      background:rgba(255,255,255,0.02);
      align-items:center; justify-content:center;
      cursor:pointer;
      border:1px solid rgba(255,255,255,0.02);
    }
    .hamburger:focus{ outline:2px solid rgba(6,182,212,0.18); }

    /* mobile menu (overlay) */
    .mobile-menu{
      position:fixed;
      inset:0;
      display:none;
      align-items:flex-start;
      justify-content:flex-end;
      z-index:9998;
    }
    .mobile-menu.open{
      display:flex;
    }
    .mobile-drawer{
      width:320px;
      max-width:92%;
      height:100vh;
      background: linear-gradient(180deg, #061122, #071525);
      padding:20px;
      box-shadow: -18px 0 40px rgba(0,0,0,0.6);
      transform: translateX(16px);
      animation: slideIn .32s cubic-bezier(.2,.9,.2,1) both;
    }
    @keyframes slideIn{ from{ transform: translateX(48px); opacity:0 } to{ transform: translateX(0); opacity:1 } }

    .mobile-nav{ display:flex; flex-direction:column; gap:8px; margin-top:12px; }
    .mobile-nav a{ padding:12px 8px; border-radius:8px; color:var(--white); text-decoration:none; display:block; }
    .mobile-nav a:hover{ background:rgba(255,255,255,0.02); transform:translateX(6px); }

    /* ========== PAGE CONTENT ========== */
    main{
      padding-top: calc(var(--nav-height) + 28px);
      max-width:var(--max-width);
      margin: 0 auto;
      padding-left:20px;
      padding-right:20px;
    }
    section{
      padding:80px 0;
      border-bottom: 1px solid rgba(255,255,255,0.02);
    }
    .hero{
      min-height:70vh;
      display:flex;
      align-items:center;
      gap:28px;
      justify-content:space-between;
    }
    .hero .left{
      max-width:640px;
    }
    h1.hero-title{
      font-size:40px;
      margin:0 0 12px 0;
      line-height:1.02;
      letter-spacing:-0.5px;
    }
    p.lead{ color:var(--muted); font-size:16px; margin:0 0 18px 0; }

    .features{
      display:grid;
      grid-template-columns: repeat(3,1fr);
      gap:18px;
      margin-top:20px;
    }
    .feature{
      padding:18px;
      border-radius:12px;
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border:1px solid rgba(255,255,255,0.02);
    }
    .feature h3{ margin:0 0 6px 0; font-size:18px; }
    .feature p{ margin:0; color:var(--muted); font-size:14px; }

    /* callout / demo image */
    .mock-card{
      width:420px;
      height:320px;
      border-radius:16px;
      background:linear-gradient(180deg,#082033,#03202b);
      box-shadow: 0 18px 50px rgba(2,6,23,0.6);
      padding:18px;
      border:1px solid rgba(255,255,255,0.03);
      display:flex;
      flex-direction:column;
      gap:12px;
    }

    /* footer */
    footer.site-footer{
      padding:40px 20px;
      color:var(--muted);
      text-align:center;
    }

    /* responsiveness */
    @media (max-width:980px){
      .features{ grid-template-columns: repeat(2,1fr); }
      .hero{ flex-direction:column-reverse; gap:16px; padding-bottom:40px; }
      .mock-card{ width:100%; max-width:420px; }
    }
    @media (max-width:640px){
      header.site-header{ height:64px; }
      .hamburger{ display:flex; }
      nav.main-nav{ display:none; }
      .features{ grid-template-columns: 1fr; }
      h1.hero-title{ font-size:28px; }
      .logo{ width:36px; height:36px; font-size:14px; }
    }

    /* subtle floating pulse on hero CTA */
    .cta-hero{ animation: float 4s ease-in-out infinite; }
    @keyframes float{ 0%{ transform:translateY(0) } 50%{ transform:translateY(-6px) } 100%{ transform:translateY(0) } }

    /* small utility */
    .muted{ color:var(--muted) }
  </style>
</head>
<body>

  <!-- ================= Header / Navigation ================= -->
  <header class="site-header" role="banner" id="siteHeader">
    <div class="nav-wrap" id="navWrap">
      <a href="#home" class="brand" aria-label="SkillCraft home">
        <span class="logo" aria-hidden="true">SC</span>
        <span class="title">
          SkillCraft
          <span class="subtitle muted">Interactive Nav Demo</span>
        </span>
      </a>

      <!-- Desktop nav -->
      <nav class="main-nav" role="navigation" aria-label="Main navigation">
        <ul class="nav" id="navList">
          <li><a href="#home" class="nav-link" data-target="home">Home</a></li>
          <li><a href="#about" class="nav-link" data-target="about">About</a></li>
          <li class="dropdown">
            <a href="#features" class="nav-link" data-target="features">Features ▾</a>
            <div class="dropdown-menu" role="menu" aria-label="More features">
              <a href="#features" role="menuitem" class="nav-link" data-target="features">All features</a>
              <a href="#ux" role="menuitem" class="nav-link" data-target="ux">UX & Accessibility</a>
              <a href="#performance" role="menuitem" class="nav-link" data-target="performance">Performance</a>
            </div>
          </li>
          <li><a href="#pricing" class="nav-link" data-target="pricing">Pricing</a></li>
          <li><a href="#contact" class="nav-link" data-target="contact">Contact</a></li>
        </ul>
      </nav>

      <!-- CTA + Hamburger -->
      <div style="display:flex;align-items:center;gap:8px">
        <button class="cta cta-hero" id="getStarted">Get Started</button>
        <button class="hamburger" id="hamburger" aria-label="Open menu" aria-expanded="false" aria-controls="mobileMenu">
          <svg width="18" height="14" viewBox="0 0 18 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <rect width="18" height="2" rx="1" fill="currentColor"></rect>
            <rect y="6" width="18" height="2" rx="1" fill="currentColor"></rect>
            <rect y="12" width="18" height="2" rx="1" fill="currentColor"></rect>
          </svg>
        </button>
      </div>
    </div>
  </header>

  <!-- Mobile drawer -->
  <div class="mobile-menu" id="mobileMenu" aria-hidden="true">
    <div class="mobile-drawer" role="dialog" aria-modal="true" aria-label="Mobile menu">
      <div style="display:flex;align-items:center;justify-content:space-between">
        <div style="display:flex;align-items:center;gap:10px">
          <div class="logo" style="width:40px;height:40px">SC</div>
          <div>
            <strong>SkillCraft</strong>
            <div class="muted" style="font-size:12px">Navigation</div>
          </div>
        </div>
        <button class="hamburger" id="mobileClose" aria-label="Close menu">
          ✕
        </button>
      </div>

      <nav class="mobile-nav" id="mobileNav" aria-label="Mobile navigation">
        <a href="#home" class="mobile-link" data-target="home">Home</a>
        <a href="#about" class="mobile-link" data-target="about">About</a>
        <a href="#features" class="mobile-link" data-target="features">Features</a>
        <a href="#pricing" class="mobile-link" data-target="pricing">Pricing</a>
        <a href="#contact" class="mobile-link" data-target="contact">Contact</a>
        <hr style="opacity:0.06;margin:12px 0" />
        <a class="mobile-link" href="#ux" data-target="ux">UX & Accessibility</a>
        <a class="mobile-link" href="#performance" data-target="performance">Performance</a>
      </nav>
    </div>
  </div>

  <!-- ================= Main content (sections used as pages) ================= -->
  <main id="mainContent" role="main">
    <section id="home" class="hero" aria-label="Home section">
      <div class="left">
        <h1 class="hero-title">Build delightful experiences — with a navigation that adapts.</h1>
        <p class="lead">This demo shows a fixed navigation menu that changes style on scroll, highlights the active section, supports dropdowns and a responsive mobile drawer. It’s accessible, keyboard-friendly, and easy to reuse across pages.</p>
        <div style="display:flex;gap:12px;margin-top:18px">
          <a href="#features" class="cta" role="button">Explore Features</a>
          <a href="#contact" class="nav-link" style="background:transparent;padding:10px 14px">Contact Us</a>
        </div>

        <div class="features" style="margin-top:28px">
          <div class="feature">
            <h3>Sticky & Responsive</h3>
            <p class="muted">The header remains visible on all sections and adapts between desktop and mobile.</p>
          </div>
          <div class="feature">
            <h3>Interactive Styles</h3>
            <p class="muted">Hover & scroll effects add depth and clear affordances for interaction.</p>
          </div>
          <div class="feature">
            <h3>Accessible</h3>
            <p class="muted">Keyboard focus states & aria attributes included for better UX.</p>
          </div>
        </div>
      </div>

      <div class="mock-card" aria-hidden="true">
        <div style="display:flex;justify-content:space-between;align-items:center">
          <div style="font-weight:700">Demo Panel</div>
          <div class="badge">Beta</div>
        </div>

        <div style="background:linear-gradient(90deg, rgba(6,182,212,0.04), rgba(124,58,237,0.03)); padding:12px; border-radius:10px;">
          <div style="font-weight:700">Interactive Nav</div>
          <div class="muted" style="font-size:13px">Scroll, click, and resize to see behavior</div>
        </div>

        <div style="flex:1;display:flex;align-items:center;justify-content:center;color:var(--muted)">
          <svg width="160" height="120" viewBox="0 0 160 120" fill="none" xmlns="http://www.w3.org/2000/svg">
            <rect x="8" y="14" width="144" height="92" rx="8" stroke="rgba(255,255,255,0.03)" fill="transparent"></rect>
            <rect x="18" y="28" width="36" height="10" rx="3" fill="rgba(255,255,255,0.03)"></rect>
            <rect x="18" y="46" width="124" height="6" rx="3" fill="rgba(255,255,255,0.02)"></rect>
            <rect x="18" y="58" width="124" height="6" rx="3" fill="rgba(255,255,255,0.02)"></rect>
          </svg>
        </div>
      </div>
    </section>

    <section id="about" aria-label="About us">
      <h2>About</h2>
      <p class="muted">This landing page is designed to be reused across site pages. The header is fixed and visible on each page, so navigation is always available.</p>
      <p class="muted">Implement the same `header` markup into other pages (e.g., about.html, pricing.html) so the menu is consistent site-wide.</p>
    </section>

    <section id="features" aria-label="Features">
      <h2>Features</h2>
      <div class="features" style="grid-template-columns:repeat(3,1fr);">
        <div class="feature"><h3>Scroll-aware</h3><p class="muted">The header changes style once you scroll a threshold (e.g., adds a backdrop and shadow).</p></div>
        <div class="feature"><h3>Hover states</h3><p class="muted">Each link has a hover 'lift' and gradient highlight to signal interactivity.</p></div>
        <div class="feature"><h3>Dropdown</h3><p class="muted">Hover a dropdown on desktop, tap on mobile — menu shows submenu items.</p></div>
      </div>
    </section>

    <section id="ux" aria-label="UX and accessibility">
      <h2>UX & Accessibility</h2>
      <p class="muted">Focus-visible outlines, skip links are recommended for production; this demo includes ARIA labels, role attributes, and keyboard operability for the mobile drawer.</p>
    </section>

    <section id="performance" aria-label="Performance">
      <h2>Performance</h2>
      <p class="muted">The header uses CSS transitions and avoids heavy paint operations. If you add a sticky effect via JS, debounce scroll handlers to avoid jank (this demo uses a minimal throttled approach).</p>
    </section>

    <section id="pricing" aria-label="Pricing">
      <h2>Pricing</h2>
      <p class="muted">Use the same nav across pricing and checkout pages to preserve orientation and reduce bounce.</p>
    </section>

    <section id="contact" aria-label="Contact">
      <h2>Contact</h2>
      <p class="muted">Reach out to start building interfaces with adaptive navigation—or copy this header into your site.</p>
      <form style="display:flex;flex-direction:column;gap:8px;max-width:520px;margin-top:12px">
        <input placeholder="Your name" style="padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:var(--white)">
        <input placeholder="Email" style="padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:var(--white)">
        <textarea placeholder="Message" rows="4" style="padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:var(--white)"></textarea>
        <div style="display:flex;gap:8px">
          <button class="cta">Send</button>
          <button class="nav-link" style="background:transparent">Reset</button>
        </div>
      </form>
    </section>

    <footer class="site-footer" aria-label="Footer">
      <div style="max-width:var(--max-width);margin:0 auto;padding:20px;color:var(--muted)">© <span id="year"></span> SkillCraft · Demo landing page with interactive navigation</div>
    </footer>
  </main>

  <!-- ================= Scripts ================= -->
  <script>
    (function(){
      // Elements
      const header = document.getElementById('siteHeader');
      const navList = document.getElementById('navList');
      const navLinks = Array.from(document.querySelectorAll('.nav-link'));
      const mobileMenu = document.getElementById('mobileMenu');
      const hamburger = document.getElementById('hamburger');
      const mobileClose = document.getElementById('mobileClose');
      const mobileLinks = Array.from(document.querySelectorAll('.mobile-link'));
      const getStarted = document.getElementById('getStarted');
      const yearEl = document.getElementById('year');

      yearEl.textContent = new Date().getFullYear();

      // Configuration
      const SCROLL_THRESHOLD = 40; // px after which header style changes
      const THROTTLE_MS = 100;     // throttle scroll handler to avoid jank

      // Utility: throttle function
      function throttle(fn, wait){
        let last = 0;
        return function(...args){
          const now = Date.now();
          if(now - last >= wait){
            last = now;
            fn.apply(this, args);
          }
        };
      }

      // Change header on scroll
      function onScroll(){
        const y = window.scrollY || window.pageYOffset;
        if(y > SCROLL_THRESHOLD){
          header.classList.add('scrolled');
        } else {
          header.classList.remove('scrolled');
        }

        // update active nav link (scrollspy)
        updateActiveLink();
      }
      window.addEventListener('scroll', throttle(onScroll, THROTTLE_MS));
      // run once
      onScroll();

      // Scrollspy: highlight link corresponding to current section in viewport
      const sections = Array.from(document.querySelectorAll('main section[id]'));

      function updateActiveLink(){
        const offset = window.scrollY + (window.innerHeight * 0.2); // tune: mark active a bit earlier
        let currentId = sections[0] && sections[0].id;
        for(const sec of sections){
          const rect = sec.getBoundingClientRect();
          const top = rect.top + window.scrollY;
          if(top <= offset) currentId = sec.id;
        }
        // Remove active from all, apply to matching nav links
        navLinks.forEach(a => {
          const target = a.dataset.target;
          if(target === currentId){
            a.classList.add('active');
            a.setAttribute('aria-current','page');
          } else {
            a.classList.remove('active');
            a.removeAttribute('aria-current');
          }
        });
        // mobile links
        mobileLinks.forEach(a => a.classList.toggle('active', a.dataset.target === currentId));
      }

      // Smooth anchor handling for nav and mobile links
      function onNavClick(e){
        // If link is internal anchor
        if(e.currentTarget && e.currentTarget.dataset && e.currentTarget.dataset.target){
          const id = e.currentTarget.dataset.target;
          const targetEl = document.getElementById(id);
          if(targetEl){
            e.preventDefault();
            // focus target element for accessibility after scroll
            const top = targetEl.getBoundingClientRect().top + window.scrollY - (parseInt(getComputedStyle(header).height) || 72) - 8;
            window.scrollTo({ top, behavior: 'smooth' });
            // If mobile open, close it
            closeMobileMenu();
          }
        }
      }
      // attach nav handlers
      navLinks.forEach(a => a.addEventListener('click', onNavClick));
      mobileLinks.forEach(a => a.addEventListener('click', onNavClick));

      // Hamburger open/close mobile menu
      function openMobileMenu(){
        mobileMenu.classList.add('open');
        mobileMenu.setAttribute('aria-hidden','false');
        hamburger.setAttribute('aria-expanded','true');
        // trap focus inside mobile menu
        const first = mobileMenu.querySelector('a,button,input,textarea');
        if(first) setTimeout(()=> first.focus(), 160);
        document.body.style.overflow = 'hidden'; // prevent background scroll
      }
      function closeMobileMenu(){
        mobileMenu.classList.remove('open');
        mobileMenu.setAttribute('aria-hidden','true');
        hamburger.setAttribute('aria-expanded','false');
        document.body.style.overflow = '';
        hamburger.focus();
      }
      hamburger.addEventListener('click', ()=> {
        const open = mobileMenu.classList.contains('open');
        if(open) closeMobileMenu(); else openMobileMenu();
      });
      mobileClose.addEventListener('click', closeMobileMenu);

      // close mobile on outside click
      mobileMenu.addEventListener('click', (e) => {
        if(e.target === mobileMenu) closeMobileMenu();
      });

      // Keyboard: Esc closes mobile menu
      window.addEventListener('keydown', (e) => {
        if(e.key === 'Escape'){
          if(mobileMenu.classList.contains('open')) closeMobileMenu();
        }
      });

      // Focus visible polyfill-ish: add focus-visible class only when keyboard used
      (function focusVisibleTracker(){
        let usingKeyboard = false;
        window.addEventListener('keydown', (e) => {
          if(e.key === 'Tab') usingKeyboard = true;
        });
        window.addEventListener('mousedown', ()=> usingKeyboard = false);
        document.addEventListener('focusin', (e) => {
          if(usingKeyboard) e.target.classList.add('focus-visible');
        });
        document.addEventListener('focusout', (e) => {
          e.target.classList.remove('focus-visible');
        });
      })();

      // CTA behavior
      getStarted.addEventListener('click', (e) => {
        // demo: scroll to features
        const target = document.getElementById('features');
        if(target){
          const top = target.getBoundingClientRect().top + window.scrollY - (parseInt(getComputedStyle(header).height) || 72) - 8;
          window.scrollTo({ top, behavior: 'smooth' });
        }
      });

      // On load, mark active link (in case we opened with hash)
      window.addEventListener('load', () => {
        if(location.hash){
          const id = location.hash.replace('#','');
          const el = document.getElementById(id);
          if(el) {
            const top = el.getBoundingClientRect().top + window.scrollY - (parseInt(getComputedStyle(header).height) || 72) - 8;
            window.scrollTo({ top, behavior: 'smooth' });
          }
        }
        updateActiveLink();
      });

      // Accessibility improvement: ensure header is visible for keyboard tabbing
      // (add a skip-to-content link if you want; not added here to keep design minimal)
    })();
  </script>
</body>
</html>
